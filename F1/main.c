#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "circuit.h"
int main()
{
    srand(time(NULL));
     printf("Bienvenue sur le projet C du groupe TEL (Tom-Emile-Louis)!\n");
    essaiLibre(600);
    return 0;
}
